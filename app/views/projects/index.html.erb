<div>

  <div>
  <%= render 'form', project: @project %>
  </div>

  <div id="projects">
    <% @projects.each do |project| %>
      <%= render 'project', project:project %>
    <% end %>
  </div>
</div>